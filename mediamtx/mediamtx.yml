# Generated by scripts/gen_mediamtx_config.sh
#
# MediaMTX is optional (docker compose --profile rtsp-proxy up -d).
# If you don't enable the profile, this file can stay as-is.
#
# Security note:
# - We bind MediaMTX to 127.0.0.1 in docker-compose.yml, so it is not reachable from LAN/Tailscale by default.
# - Only enable broader exposure if you have a specific need and firewall it accordingly.

logLevel: info

paths: {}
